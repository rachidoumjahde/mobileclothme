import 'package:flutter/material.dart';


class FitReviewDetails extends StatelessWidget {

  final String likesNumberMessage;

  FitReviewDetails({Key key, this.likesNumberMessage, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Row(),
          Row()
        ],
      ),
    );
  }
}